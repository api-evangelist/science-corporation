generated: '2026-07-21'
method: derived
source: grpc/science-corporation-synapse.proto
description: >-
  Cross-cutting standards conformance for the Synapse Protocol, derived from the published
  protobuf service definitions. Synapse is a gRPC/Protobuf device API; it does not implement
  web-API standards such as OAuth2/OIDC, REST pagination, or RFC 9457 problem details.
standards:
- id: grpc
  conforms: true
  evidence: SynapseDevice service defined in proto3; gRPC over HTTP/2 control plane.
- id: protobuf-proto3
  conforms: true
  evidence: All api/*.proto files declare syntax = "proto3".
- id: grpc-server-streaming
  conforms: true
  evidence: StreamQuery, ReadFile, TailLogs, DeployApp use `stream` responses.
- id: ndtp-udp-streaming
  conforms: true
  evidence: Data plane uses the Neural Data Transport Protocol (NDTP) over UDP.
- id: oauth2
  conforms: false
  evidence: No OAuth2 security scheme; device-local access with no documented auth.
- id: oidc
  conforms: false
- id: rfc9457-problem-details
  conforms: false
  evidence: Errors use a protobuf Status message (StatusCode enum), not application/problem+json.
- id: rest-pagination
  conforms: false
  evidence: gRPC device API; no HTTP collection pagination.
- id: fhir-r4
  conforms: false
